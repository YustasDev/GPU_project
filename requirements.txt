# requirements.txt — зависимости проекта Smart Observer (версии ЗАФИКСИРОВАНЫ).
# Версии совпадают с протестированным окружением ai_project — сборка образа
# воспроизводима: и сегодня, и через год установятся ровно эти версии.

# YOLO-детекция
ultralytics==8.4.18

# torch/torchvision — сборка под CUDA 12.1 (локальная метка +cu121). Эти пакеты
# берутся НЕ с PyPI, а с индекса PyTorch — он подключён в Dockerfile
# (--extra-index-url https://download.pytorch.org/whl/cu121).
torch==2.5.1+cu121
torchvision==0.20.1+cu121

# Работа с изображениями и видео (модуль cv2)
opencv-python==4.13.0.92

# Численные операции (фиксируем явно — база для torch/opencv/ultralytics)
numpy==2.3.5

# База данных
SQLAlchemy==2.0.47
psycopg2-binary==2.9.11   # драйвер PostgreSQL «в бинарнике»: не требует gcc/libpq-dev при сборке
alembic==1.18.4           # миграции схемы: добавление колонки description (см. §11.7)

# Веб-дашборд
fastapi==0.133.1
uvicorn[standard]==0.41.0
jinja2==3.1.6

# LLM-обработка и конфигурация
openai==2.43.0
httpx==0.28.1
python-dotenv==1.2.2
